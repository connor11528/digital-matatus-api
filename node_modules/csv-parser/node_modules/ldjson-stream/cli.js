#!/usr/bin/env node
var stream = require('./')()
var count = 0
var limit = 5200000
process.stdin.pipe(stream)
  .on('data', function(c) {
    // console.log(c.toString())
    count++
    if ((count % (limit / 100)) === 0) {
      var percent = count/limit * 100
      // console.log(~~percent + '%')
    }
  })
  .on('end', function() {
    console.log(count)
  })
